# golang-updater
Simple & fast Golang Updater for Linux written in rust

# Build the binary
```shell
cargo build --release
```

# Make executable
```shell
sudo chmod +x path/to/the/go_updater_binary
```
